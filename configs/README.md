Configuration files for reusable corpus-building runs should live here.

Prefer TOML files that define project settings, query inventories, source adapters, normalization options, and export settings.
